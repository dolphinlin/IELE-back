<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">網站資訊</h3>
  </div>
  <div class="panel-body">
    <form method="post">
      <div class="form-group">
        <label for="origin">緣起</label>
        <textarea class="form-control" name="origin" rows="8" cols="80">{{origin}}</textarea>
      </div>
      <div class="form-group">
        <label for="date">舉辦日期、地點</label>
        <textarea name="date" rows="8" cols="80" class="form-control">{{date}}</textarea>
      </div>
      <div class="form-group">
        <label for="topic">徵稿主題（一行代表一項目）</label>
        <textarea name="topic" rows="8" cols="80" class="form-control">{{topic}}</textarea>
      </div>
      <div class="form-group">
        <label for="notic">重要日期（一行代表一項目）</label>
        <textarea name="notic" rows="8" cols="80" class="form-control">{{notic}}</textarea>
      </div>
      <div class="form-group">
        <label for="banner">上方廣告欄（圖片網址，請自行上傳）<a href="http://imgur.com/" target="_blank">Imgur</a></label>
        <input name="banner" class="form-control" type="text" value="{{banner}}"></input>
      </div>
      <div class="form-group">
        <button class="btn btn-primary btn-block">送出</button>
      </div>
    </form>
  </div>
</div>
